from src.config.security import (
    VULN_CLASSES,
    LANGUAGE_CLASSES
)

EMBED_DIM = 128

NUM_HEADS = 4

FF_DIM = 256

NUM_LAYERS = 2

MAX_LEN = 128

PAD_IDX = 0

LANGUAGE_NUM_CLASSES = len(
    LANGUAGE_CLASSES
)

VULN_NUM_CLASSES = len(
    VULN_CLASSES
)